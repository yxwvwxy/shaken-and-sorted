import SwiftUI

enum AppTheme {
    /// Deep bar green with warm amber accent — cocktail journal, not purple/cream defaults.
    static let background = Color(red: 0.07, green: 0.11, blue: 0.10)
    static let card = Color(red: 0.12, green: 0.17, blue: 0.15)
    static let photoPlaceholder = Color(red: 0.16, green: 0.22, blue: 0.20)
    static let accent = Color(red: 0.89, green: 0.62, blue: 0.28)
    static let primaryText = Color(red: 0.96, green: 0.94, blue: 0.90)
    static let secondaryText = Color(red: 0.72, green: 0.74, blue: 0.70)
    static let tertiaryText = Color(red: 0.52, green: 0.56, blue: 0.53)
}
